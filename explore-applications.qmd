---
output: html_document
editor_options: 
  chunk_output_type: console
---
# Anwendungen: Erkunden {#sec-explore-applications}

```{r}
#| include: false
source("_common.R")
```

\vspace{10mm}

## Fallstudie: Effektive Kommunikation explorativer Ergebnisse {#sec-case-study-effective-comms}

Grafiken können Ideen direkt und schnell wirkungsvoll kommunizieren.
Wir alle wissen schließlich, dass „ein Bild mehr sagt als tausend Worte". Leider gibt es jedoch Zeiten, in denen ein Bild eine Botschaft vermittelt, die ungenau oder irreführend ist.

Dieses Kapitel befasst sich damit, wie Grafiken am besten genutzt werden können, um Daten genau und effektiv darzustellen.
Neben der Datenmodellierung ist die kreative Visualisierung gewissermaßen eine Kunst.
Aber auch bei einer Kunst gibt es empfohlene Leitprinzipien.
Wir stellen einige Best Practices für die Erstellung von Datenvisualisierungen vor.

### Einfach halten

Behalten Sie beim Erstellen einer Grafik im Blick, was Sie Ihrem Leser zeigen möchten.
Farben sollten verwendet werden, um Elemente zu gruppieren oder Ausprägungen auf sinnvolle Weise zu unterscheiden.
Farben können ablenkend sein, wenn sie nur verwendet werden, um die Grafik aufzuhellen.

Stellen Sie sich ein Fertigungsunternehmen vor, das seine Kosten in fünf verschiedene Kategorien zusammengefasst hat.
Beachten Sie bei den beiden Grafiken in @fig-pie-to-bar, dass die Größen im Kreisdiagramm in @fig-pie-to-bar-1 für das Auge schwer zu vergleichen sind.
Können Ihre Augen erkennen, wie verschieden „Gebäude und Verwaltung" von „Arbeitsmaterialien" ist, wenn Sie die Tortenstücke betrachten?
Außerdem bedeuten die Farben im Kreisdiagramm nichts und sind daher ablenkend.
Schließlich verbessert der dreidimensionale Aspekt des Bildes nicht die Fähigkeit des Lesers, die dargestellten Daten zu verstehen.

Als Alternative wird in @fig-pie-to-bar-2 ein Balkendiagramm bereitgestellt.
Beachten Sie, wie viel einfacher es ist, die Größe der Unterschiede zwischen den Kategorien zu erkennen, ohne von anderen Aspekten des Bildes abgelenkt zu werden.
In der Regel ist ein Balkendiagramm für den Leser leichter verständlich als ein Kreisdiagramm, insbesondere wenn die dargestellten kategorialen Daten mehr als nur wenige Ausprägungen haben.

\clearpage

```{r}
expenses <- tribble(
  ~category,                     ~value,
  "Cutting tools"                , 0.03,
  "Buildings and Administration" , 0.22,
  "Labor"                        , 0.31,
  "Machinery"                    , 0.27,
  "Workplace materials"          , 0.17
) |>
  mutate(value =  value * 100) |>
  uncount(weights = value)
```

```{r}
#| label: fig-pie-to-bar
#| fig-cap: Same information displayed with two very different visualizations. 
#| fig-subcap: 
#|   - A three-dimensional pie chart.
#|   - A bar plot.
#| fig-alt: |
#|   A three dimensional pie chart and a bar plot. Both plots show that the 
#|   biggest sources of cost are labor, machinery, and buildings & administration,
#|   in that order. Very little of the costs are due to cutting tools.
#| fig-width: 3.5
#| layout: [[40,60]]
knitr::include_graphics("images/pie-3d.jpg")
ggplot(expenses, aes(x = fct_infreq(category))) +
  geom_bar() +
  theme_minimal() +
  coord_flip() +
  labs(x = NULL, y = NULL)
```

### Farbe verwenden, um Aufmerksamkeit zu lenken

Es gibt viele Gründe, warum Sie Ihren Grafiken **Farbe** hinzufügen könnten.
Ein wichtiges Prinzip, das man im Hinterkopf behalten sollte, ist, Farbe einzusetzen, um Aufmerksamkeit zu lenken.
Natürlich sollten Sie noch immer daran denken, wie visuell ansprechend Ihre Visualisierung ist, und wenn Sie Farbe hinzufügen, um sie optisch ansprechender zu gestalten, ohne die Aufmerksamkeit auf ein bestimmtes Merkmal zu lenken, ist das möglicherweise in Ordnung.
Sie sollten jedoch die Standardfarben kritisch hinterfragen und explizit entscheiden, ob und wie Farbe eingesetzt werden soll.
Beachten Sie, dass in @fig-red-bar-2 die Farbgebung so gewählt wurde, dass sie die Aufmerksamkeit des Lesers auf eine bestimmte Information lenkt.
Die Standardfarbgebung in @fig-red-bar-1 kann ablenkend sein und den Leser fragen lassen, ob es beispielsweise eine Gemeinsamkeit zwischen den roten und lila Balken gibt.
Beachten Sie auch, dass nicht jeder Farben gleich wahrnimmt; oft ist es nützlich, Farbe und ein weiteres Merkmal (z.B. ein Muster) hinzuzufügen, damit Sie auf die Merkmale, auf die Sie aufmerksam machen möchten, auf verschiedene Weisen hinweisen können, wie in @fig-red-bar-3 dargestellt.

\vspace{-2mm}

```{r}
#| label: fig-red-bar
#| fig-cap: |
#|   Three bar charts visualizing the same information with different coloring 
#|   to highlight different aspects.
#| fig-subcap: 
#|   - Default coloring does nothing for the understanding of the data.
#|   - Color draws attention directly to the bar on Buildings and Administration.
#|   - Color and linetype draw attention directly to the bar on Buildings and Administration.
#| fig-alt: |
#|   Three bar charts visualizing the same information with different coloring 
#|   to highlight different aspects. First plot colors each bar (Cutting tools,
#|   Workspace materials, Buildings and Administration, Machinery, and Labor)
#|   differently, while in the second and third plots Buildings and 
#|   Administration is highlighted in red and the rest of the bars are grey.
#| fig-width: 4.0
#| fig-asp: 0.57
#| out-width: 100%
#| layout: [[48,-4,48],[48,-4,48]]
ggplot(expenses, aes(y = fct_infreq(category), fill = category)) +
  geom_bar(show.legend = FALSE) +
  scale_fill_openintro("five") +
  labs(x = NULL, y = NULL) +
  scale_y_discrete(labels = label_wrap(14))

expenses |>
  mutate(highlight = if_else(category == "Buildings and Administration", "yes", "no")) |>
  ggplot(aes(y = fct_infreq(category), fill = highlight)) +
  geom_bar(show.legend = FALSE) +
  scale_fill_manual(values = c(IMSCOL["lgray", "full"], IMSCOL["red", "full"])) +
  labs(x = NULL, y = NULL) +
  scale_y_discrete(labels = label_wrap(14))

expenses |>
  mutate(highlight = if_else(category == "Buildings and Administration", "yes", "no")) |>
  ggplot(aes(y = fct_infreq(category), fill = highlight, color = highlight, linetype = highlight, size = highlight)) +
  geom_bar(show.legend = FALSE) +
  scale_fill_manual(values = c(IMSCOL["lgray", "full"], IMSCOL["red", "full"])) +
  labs(x = NULL, y = NULL) +
  scale_color_manual(values = c(IMSCOL["lgray", "full"], "white")) +
  scale_size_manual(values = c(0, 0.8)) +
  scale_y_discrete(labels = label_wrap(14))
```

\clearpage

### Eine Geschichte erzählen

Bei vielen Grafiken ist die Einbeziehung von Informationen, die nicht im dargestellten Datensatz enthalten sind, ein wichtiger Aspekt.
Die externen Informationen dienen dazu, die Daten in einen Kontext zu stellen und die Geschichte der Forschung zu kommunizieren.

In @fig-duke-hires ist die rechte Grafik mit Informationen über den Beginn des Geschäftsjahres der Universität **kommentiert**, was die durch die Daten bereitgestellten Informationen kontextualisiert.
Manchmal können die zusätzlichen Informationen auch eine diagonale Linie sein, die durch $y = x$ gegeben ist; Punkte oberhalb der Linie zeigen dem Leser schnell, welche Werte eine $y$-Koordinate haben, die größer ist als die $x$-Koordinate; Punkte unterhalb der Linie zeigen das Gegenteil.

```{r}
#| label: fig-duke-hires
#| fig-cap: |
#|   Time series plot showing monthly Duke University hiring trends 
#|   over five calendar years.
#| fig-alp: |
#|   Time series plot showing monthly Duke University hiring trends 
#|   over five calendar years. Hiring spikes in July.
#| fig-subcap: 
#|   - Colored by year
#|   - Same color for all years, annotation summarizing trend
#| out-width: 100%
#| fig-asp: 0.3
#| fig-width: 10
duke_hires_raw <- read_csv("data/duke-hires.csv")

set.seed(1234)

duke_hires <- duke_hires_raw |>
  mutate(
    `2011` = `2010` + round(runif(12, min = -30, max = 15)),
    `2011` = `2010` + round(runif(12, min = -12, max = 10) * 1.02),
    `2012` = `2010` + round(runif(12, min = -11, max = 31) * 0.95),
    `2013` = `2010` + round(runif(12, min = -37, max = 20) * 1.1),
    `2014` = `2010` + round(runif(12, min = -10, max = 42) * 0.8),
    `2015` = `2010` + round(runif(12, min = -23, max = 18) * 1.5),
    `2012` = if_else(month == 6, 600, `2012`),
    `2013` = if_else(month == 6, 480, `2013`),
    `2014` = if_else(month == 6, 550, `2014`),
    `2015` = if_else(month == 6, 430, `2015`),
    `2012` = if_else(month == 8, 600 + 10, `2012`),
    `2013` = if_else(month == 8, 480 + 80, `2013`),
    `2014` = if_else(month == 8, 550 + 90, `2014`),
    `2015` = if_else(month == 8, 430 + 140, `2015`),
    `2015` = if_else(month == 7, 800, `2015`)
  ) |>
  pivot_longer(
    cols = !month,
    names_to = "year",
    values_to = "n"
  )

ggplot(duke_hires, aes(x = month, y = n, color = year)) +
  geom_line(linewidth = 1) +
  scale_color_openintro("five") +
  scale_x_continuous(breaks = 1:12, labels = month.abb, minor_breaks = NULL) +
  scale_y_continuous(breaks = seq(0, 800, 100), minor_breaks = NULL) +
  labs(
    title = "Duke hires by month",
    y = NULL, x = NULL, color = NULL
  ) +
  theme(
    legend.position = c(0.12, 0.65),
    legend.background = element_rect(color = "gray")
  )

ggplot(duke_hires, aes(x = month, y = n, group = year)) +
  geom_segment(x = 7, xend = 7, y = 0, yend = 800) +
  geom_line(linewidth = 0.8, color = IMSCOL["blue", "f2"]) +
  scale_x_continuous(breaks = 1:12, labels = month.abb, minor_breaks = NULL) +
  scale_y_continuous(breaks = seq(0, 800, 200), minor_breaks = NULL) +
  labs(
    title = "Duke hires by month",
    y = NULL, x = NULL
  ) +
  annotate(
    "label",
    x = 7, y = 250,
    label = "Hires always peak in\nJuly, which is the start\nof Duke's fiscal year.",
    hjust = -0.1
  )
```

### Reihenfolge ist wichtig

Die meisten Softwareprogramme haben eingebaute Methoden für einige Grafikdetails – manche ordnen Ausprägungen alphabetisch, andere bieten Funktionalitäten zum Anordnen in einer benutzerdefinierten Reihenfolge.
Wie in @fig-brexit-bars-1 zu sehen ist, ist die alphabetische Anordnung für die Beschreibung der Daten nicht besonders bedeutsam.
Manchmal ist es sinnvoll, die Balken vom höchsten zum niedrigsten (oder umgekehrt) zu **ordnen**, wie in @fig-brexit-bars-2 dargestellt.
In diesem Fall ist die beste Anordnung jedoch wahrscheinlich die, in der die Fragen gestellt wurden, wie in @fig-brexit-bars-3 gezeigt.
Eine Anordnung, die im Kontext des Problems keinen Sinn ergibt (z.B. hier alphabetisch), kann den Leser irreführen, der möglicherweise nur einen kurzen Blick auf die Achsen wirft und die Balkenbeschriftungen nicht sorgfältig liest.

Im September 2019 befragte eine YouGov-Umfrage 1.639 Erwachsene in Großbritannien die folgende Frage[^06-explore-applications-1]:

[^06-explore-applications-1]: Quelle: [YouGov Survey Results](https://d25d2506sfb94s.cloudfront.net/cumulus_uploads/document/x0msmggx08/YouGov%20-%20Brexit%20and%202019%20election.pdf), abgerufen am 7. Oktober 2019.

\vspace{3mm}

> Wie gut oder schlecht denken Sie, macht die Regierung ihre Arbeit beim Umgang mit dem Austritt Großbritanniens aus der Europäischen Union?
>
> -   Sehr gut
> -   Ziemlich gut
> -   Ziemlich schlecht
> -   Sehr schlecht
> -   Weiß nicht

\clearpage

```{r}
brexit <- tibble(
  opinion = c(
    rep("Very well", 123), rep("Fairly well", 219), rep("Fairly badly", 332),
    rep("Very badly", 818), rep("Don't know", 151)
    ),
  region = c(
    rep("london", 10), rep("rest_of_south", 49), rep("midlands_wales", 32), rep("north", 24), rep("scot", 8),
    rep("london", 18), rep("rest_of_south", 93), rep("midlands_wales", 50), rep("north", 48), rep("scot", 10),
    rep("london", 33), rep("rest_of_south", 126), rep("midlands_wales", 74), rep("north", 76), rep("scot", 23),
    rep("london", 118), rep("rest_of_south", 246), rep("midlands_wales", 152), rep("north", 212), rep("scot", 90),
    rep("london", 16), rep("rest_of_south", 38), rep("midlands_wales", 46), rep("north", 40), rep("scot", 11)
    )
)
```

```{r}
#| label: fig-brexit-bars
#| fig-cap: |
#|   Three bar charts visualizing the same information with arrangement of levels.
#| fig-subcap: 
#|   - Alphabetic order
#|   - Frequecy order
#|   - Same order as presented in the survey question
#| fig-alt: |
#|   Three bar plots with the bars (Very well, Fairly well, Fairly badly, 
#|   Very badly, Don't know) arranged differently in each plot. In the first 
#|   plot they're in alphabetical order, in the second in frequency order 
#|   (highest Very badly to lowest Very well), and in the third plot in the 
#|   same order as presented in the survey question.
#| fig-width: 4.0
#| out-width: 100%
#| layout-ncol: 2
#| fig-asp: 0.5
ggplot(brexit, aes(y = fct_rev(opinion))) +
  geom_bar() + 
  labs(y = "Opinion", x = "Count")

ggplot(brexit, aes(y = fct_rev(fct_infreq(opinion)))) +
  geom_bar() + 
  labs(y = "Opinion", x = "Count")

brexit |>
  mutate(opinion = fct_relevel(opinion, "Very well", "Fairly well", "Fairly badly", "Very badly", "Don't know")) |>
  ggplot(aes(y = opinion)) +
  geom_bar() + 
  labs(y = "Opinion", x = "Count")
```

### Beschriftungen so lesbar wie möglich gestalten

Die Brexit-Umfrageergebnisse wurden zusätzlich nach Region in Großbritannien aufgeschlüsselt.
Das gestapelte Balkendiagramm ermöglicht den Vergleich der Brexit-Meinung über die fünf Regionen.
In @fig-brexit-region-1 sind die Balken vertikal und in @fig-brexit-region-2 horizontal.
Obwohl die quantitative Information in den beiden Grafiken identisch ist, bietet das Umdrehen der Grafik und das Erstellen horizontaler Balken mehr Platz für die **Achsenbeschriftungen**.
Je leichter die Kategorien zu lesen sind, desto mehr wird der Leser aus der Visualisierung lernen.
Denken Sie daran: Das Ziel ist es, so viele Informationen wie möglich auf prägnante und klare Weise zu vermitteln.

```{r}
brexit <- brexit |>
  mutate(
    region = fct_relevel(
      region,
      "london", "rest_of_south", "midlands_wales", "north", "scot"
    ),
    region = fct_recode(
      region,
      London = "london", 
      `Rest of South` = "rest_of_south", 
      `Midlands / Wales` = "midlands_wales", 
      North = "north", 
      Scotland = "scot"
    )
  ) |>
  mutate(Opinion = fct_relevel(opinion, c("Very well", "Fairly well", "Fairly badly", "Very badly", "Don't know")))
```

::: {#fig-brexit-region layout="[[100], [100]]"}

```{r}
#| label: fig-brexit-region-1
#| fig-cap: Vertical bars across the x-axis.
#| fig-alt: |
#|   Stacked bar plot of region and opinion, where vertical bars are on 
#|   the x-axis.
#| fig-asp: 0.25
#| out-width: 100%
ggplot(brexit, aes(x = region, fill = Opinion)) +
  geom_bar(show.legend = FALSE) +
  scale_fill_openintro("five") +
  labs(x = "Region", y = "Count")
```

```{r}
#| label: fig-brexit-region-2
#| fig-cap: Horizontal bars across the y-axis.
#| fig-alt: |
#|   Stacked bar plot of region and opinion, where horizontal bars are on 
#|   the y-axis.
#| fig-asp: 0.3
#| out-width: 100%
ggplot(brexit, aes(y = region, fill = Opinion)) +
  geom_bar() +
  labs(x = "Count", y = NULL) +
  scale_fill_openintro("five") +
  theme(legend.position = "bottom")
```

Gestapelte Balkendiagramme. Die horizontale Ausrichtung macht die Regionsbeschriftungen leichter lesbar.

:::

\clearpage

### Einen Zweck wählen

Jede grafische Entscheidung sollte mit einem **Zweck** getroffen werden.
Wie bereits erwähnt, ist das Festhalten an Standardoptionen nicht immer das Beste, um die Geschichte Ihrer Daten zu erzählen.
Gestapelte Balkendiagramme erzählen einen Teil einer Geschichte.
Je nach Forschungsfrage erzählen sie möglicherweise nicht den für die Forschung wichtigsten Teil der Geschichte.

@fig-seg-three-ways bietet drei verschiedene Möglichkeiten, dieselben Informationen darzustellen.
Wenn der wichtigste Vergleich zwischen Regionen der Anteil ist, bevorzugen Sie möglicherweise @fig-seg-three-ways-1. Wenn der wichtigste Vergleich zwischen Regionen auch die Gesamtzahl der Personen in der Region berücksichtigt, bevorzugen Sie möglicherweise @fig-seg-three-ways-2. Wenn ein separates Balkendiagramm für jede Region den Punkt verdeutlicht, den Sie vermitteln möchten, verwenden Sie @fig-seg-three-ways-3, das nach Region **facettiert** wurde.
@fig-seg-three-ways-3 enthält auch vollständige Titel und eine prägnante URL mit der Datenquelle.
Weitere bewusste Entscheidungen umfassen die Verwendung informativer Beschriftungen und die Vermeidung von Redundanz.

::: {#fig-seg-three-ways layout="[[100], [100], [100]]"}

```{r}
#| label: fig-seg-three-ways-1
#| fig-cap: Stacked bar plot of region and opinion, showing percentages.
#| fig-alt: Stacked bar plot of region and opinion, showing percentages.
#| fig-asp: 0.3
#| out-width: 100%
ggplot(brexit, aes(y = region,  fill = Opinion)) +
  geom_bar(position = "fill") +
  labs(x = "Percent", y = NULL) +
  scale_fill_openintro("five") +
  scale_x_continuous(labels = label_percent()) +
  theme(legend.position = "top")
```

```{r}
#| label: fig-seg-three-ways-2
#| fig-cap: Stacked bar plot of region and opinion, showing counts.
#| fig-alt: Stacked bar plot of region and opinion, showing counts.
#| fig-asp: 0.3
#| out-width: 100%
ggplot(brexit, aes(y = region, fill = Opinion)) +
  geom_bar() + 
  labs(x = "Count", y = NULL) +
  scale_fill_openintro("five") +
  theme(legend.position = "top")
```

```{r}
#| label: fig-seg-three-ways-3
#| fig-cap: Dodged bar plot of region and opinion, showing counts.
#| fig-alt: Dodged bar plot of region and opinion, showing counts.
#| fig-asp: 0.4
#| out-width: 100%
#| fig-width: 10
ggplot(brexit, aes(y = Opinion)) +
  geom_bar() +
  facet_grid(. ~ region, labeller = label_wrap_gen(width = 12)) +
  scale_x_continuous(breaks = c(0, 100, 200)) +
  labs(
    title = "How well or badly do you think the government are doing at handling Britain's exit\nfrom the European Union?",
    subtitle = "YouGov Survey Results, 2-3 September 2019",
    caption = "Source: bit.ly/2lCJZVg", 
    x = NULL, y = NULL
  ) +
  theme(plot.title.position = "plot")
```

Drei verschiedene Darstellungen zweier Variablen aus der Umfrage, Region und Meinung.
:::

\clearpage

### Bedeutungsvolle Farben auswählen

Eine letzte Überlegung beim Erstellen von Grafiken ist die Wahl der Farben.
Standard- oder Regenbogenfarben sind nicht immer die beste Wahl, um die Ausprägungen Ihrer Variablen am deutlichsten zu unterscheiden.
Es wurde viel Forschung betrieben, um Farbkombinationen zu finden, die unterscheidbar und für Menschen mit unterschiedlichem Farbsehen klar sind.
Die Cividis-Skala eignet sich gut für ordinale Daten.
[@Nunez:2018] @fig-brexit-viridis zeigt denselben Plot mit zwei verschiedenen Farbthemen.

\vspace{5mm}

```{r}
#| label: fig-brexit-viridis
#| fig-cap: Identical bar plots with two different coloring options.
#| fig-subcap:
#|   - Default color scale
#|   - Cividis scale
#| fig-alt: Identical bar plots with two different coloring options. 
#| fig-asp: 0.4
#| out-width: 100%
p <- ggplot(brexit, aes(y = region, fill = Opinion)) +
  geom_bar(position = "fill") +
  labs(
    title = "How well or badly do you think the government are doing at handling Britain's exit\nfrom the European Union?",
    subtitle = "YouGov Survey Results, 2-3 September 2019",
    caption = "Source: bit.ly/2lCJZVg",
    x = NULL, y = NULL, fill = NULL
  ) +
  theme(plot.title.position = "plot")

p +
  scale_fill_openintro("five")

p +
  scale_fill_viridis_d(option = "E")
```

\vspace{5mm}

In diesem Kapitel werden verschiedene Darstellungen gegenübergestellt, um Best Practices bei der Erstellung von Grafiken zu demonstrieren.
Das grundlegende Prinzip ist, dass Ihre Grafik maximale Informationen prägnant und klar vermitteln sollte.
Beschriftungen sollten klar und horizontal für den Leser ausgerichtet sein.
Vergessen Sie keine Titel und fügen Sie wenn möglich die Datenquelle hinzu.

\clearpage

## Interaktive R-Tutorials {#sec-explore-tutorials}

Erkunden Sie die in diesem Teil erlernten Konzepte in R mithilfe der folgenden Selbstlern-Tutorials.
Sie benötigen lediglich Ihren Browser, um loszulegen!

::: {.alltutorials data-latex=""}
[Tutorial 2: Explorative Datenanalyse](https://openintrostat.github.io/ims-tutorials/02-explore/)

::: {.content-hidden unless-format="pdf"}
<https://openintrostat.github.io/ims-tutorials/02-explore>
:::
:::

::: {.singletutorial data-latex=""}
[Tutorial 2 – Lektion 1: Visualisierung kategorialer Daten](https://openintro.shinyapps.io/ims-02-explore-01/)

::: {.content-hidden unless-format="pdf"}
<https://openintro.shinyapps.io/ims-02-explore-01>
:::
:::

::: {.singletutorial data-latex=""}
[Tutorial 2 – Lektion 2: Visualisierung numerischer Daten](https://openintro.shinyapps.io/ims-02-explore-02/)

::: {.content-hidden unless-format="pdf"}
<https://openintro.shinyapps.io/ims-02-explore-02>
:::
:::

::: {.singletutorial data-latex=""}
[Tutorial 2 – Lektion 3: Zusammenfassen mit Statistiken](https://openintro.shinyapps.io/ims-02-explore-03/)

::: {.content-hidden unless-format="pdf"}
<https://openintro.shinyapps.io/ims-02-explore-03>
:::
:::

::: {.singletutorial data-latex=""}
[Tutorial 2 – Lektion 4: Fallstudie](https://openintro.shinyapps.io/ims-02-explore-04/)

::: {.content-hidden unless-format="pdf"}
<https://openintro.shinyapps.io/ims-02-explore-04>
:::
:::

::: {.content-hidden unless-format="pdf"}
Sie können auch auf die vollständige Liste der Tutorials für dieses Buch unter <https://openintrostat.github.io/ims-tutorials> zugreifen.
:::

::: {.content-visible when-format="html"}
Sie können auch [hier](https://openintrostat.github.io/ims-tutorials) auf die vollständige Liste der Tutorials für dieses Buch zugreifen.
:::

## R-Labs {#sec-explore-labs}

Wenden Sie die in diesem Teil erlernten Konzepte in R mit computergestützten Labs an, die Sie durch eine Datenanalyse-Fallstudie führen.

::: {.singlelab data-latex=""}
[Einführung in Daten – Flugverspätungen](https://www.openintro.org/go?id=ims-r-lab-intro-to-data)\

::: {.content-hidden unless-format="pdf"}
<https://www.openintro.org/go?id=ims-r-lab-intro-to-data>
:::
:::

::: {.content-hidden unless-format="pdf"}
Sie können auch auf die vollständige Liste der Labs für dieses Buch unter <https://www.openintro.org/go?id=ims-r-labs> zugreifen.
:::

::: {.content-visible when-format="html"}
Sie können auch [hier](https://www.openintro.org/go?id=ims-r-labs) auf die vollständige Liste der Labs für dieses Buch zugreifen.
:::